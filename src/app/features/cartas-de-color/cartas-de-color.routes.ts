import { Routes } from '@angular/router';
import { CartasDeColor } from './feature/cartas-de-color';

export const CARTAS_DE_COLOR_ROUTES: Routes = [
  {
    path: '',
    component: CartasDeColor,
  },
];
